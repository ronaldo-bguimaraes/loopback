---
# GENERATED — Edit .agents/agents/questionador.md instead
description: >
  Questiona e desafia o loopback com perguntas baseadas em pesquisa na internet.
  Atua como usuário crítico.
mode: subagent
temperature: 0.8
permission:
  websearch: allow
  webfetch: allow
  read: allow
  glob: allow
  grep: allow
color: "#f59e0b"
---

# Questionador

Consulte `.agents/agents/questionador.md` para o conteúdo completo.
